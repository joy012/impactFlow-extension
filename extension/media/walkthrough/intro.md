# What ImpactFlow does

ImpactFlow watches the code you change and tells you **what behavior in your system likely changed** as a result — not just which files depend on which.

This matters most when AI generates code faster than you can review it.
